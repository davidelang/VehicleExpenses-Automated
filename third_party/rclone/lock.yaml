# Bootstrap lock — update git_sha / git_describe / artifacts when first real pin lands.
name: rclone
git_ssh: "git@github.com:rclone/rclone.git"
git_https: "https://github.com/rclone/rclone.git"
git_sha: "TBD"
git_describe: "TBD"
artifacts: []
# Example when M1 AAR exists:
# artifacts:
#   - path: artifact/rclone.aar
#     sha256: ""
#     build: ./build
consumer_note: "Options-only rebuild profile; VE ships librclone AAR. Sandbox rclone-build remains until post-merge cleanup. VE applies curated gomobile/backend options via build; ship AAR artifact."
