#!/usr/bin/env bash
# Placeholder: full paddle Android Docker rebuild is multi-hour; see SOURCE.md + docs/specs/PADDLE_BUILD.md
set -euo pipefail
SANDBOX="${PADDLE_BUILD_DIR:-/home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/paddle-build}"
echo "paddle build not fully automated in third_party yet."
echo "Use sandbox recipes: $SANDBOX"
echo "Docs: docs/specs/PADDLE_BUILD.md"
ls -la "$SANDBOX" | head -20
exit 1
