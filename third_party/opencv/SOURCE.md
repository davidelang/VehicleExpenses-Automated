# OpenCV third_party pin (planned)

- Host: `~/git/opencv` (tag/branch 4.10.0) + `ve-build/README.md` for 16KB page goals
- ABIs: arm64-v8a + x86_64; drop plain x86; armv7 only if needed
- Until pin lands, app uses `org.opencv:opencv:4.10.0` and checked-in `jniLibs`
