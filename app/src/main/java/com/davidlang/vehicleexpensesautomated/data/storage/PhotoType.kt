package com.davidlang.vehicleexpensesautomated.data.storage

enum class PhotoType {
    FUEL,           // odometer / pump / fill-up photos
    EXPENSE,        // receipt / expense photos
    DASH_REFERENCE  // dedicated reference dashboard photos for vehicles
}
