# TODO

- [ ] Execution of pump-experiment-descriptive-column-labels-stretch-blue-method-20260616-plan.md (VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs) + all prior failure logs read + record lessons on identical first-action skips; compliance re-read of STANDARD BLOCK first 10 lines + last 5 current-state + git --porcelain no .kt + grep TODO pre (no match); narrow forensic read_file (offset/limit focused on the flows list + full preceding // comment block, e.g. 240 limit 20 and 230 limit 40) + targeted grep for current plain "Set A", "Set B" etc. in the list and for stretch/blue terms to confirm mapping; also narrow read the pBuildHtmlHeader (~2342) and pBuildHtmlRowDynamic (~2355) focusing on how `flows` / `name` strings are interpolated into labels; re-read first 10 lines of STANDARD BLOCK + last lines of current-state.md; confirm no prior .kt edits in this sub turn; ONLY after 100% gates: update TODO first (this) + verifs + baseline git add .kt + TODO + ./build_app; then Phases 1-2 ultra-micro per plan (Phase 0: re-reads + hygiene + TODO + baseline forensic + build (no .kt edit); Phase 1: the single mechanical edit on flows list + its documentation comment; Phase 2: final forensic + handoff build); all gates/forensic/grep before/after every edit, git add the .kt + TODO.md before each build; scope strictly the `flows` list declaration + its immediately preceding multi-line `//` documentation comment block in ExperimentPumpScreen.kt (A remains exactly "Set A"; B–G get the exact descriptive suffixes; one short high-signal addition to the comment documenting the new label convention + mapping) — NO edits anywhere else (no pBuild*, no procs, no helpers, no other comments or code); 3-3-3 + anti-doom + literal preflight only via get-builds-tag.sh; end with exact END marker + results ready (tag) + this plan path)
- [ ] Execution of add-set-f-g-raw-clones-of-b-d-skip-contrast-stretch (approved session plan /home/dlang/.grok/sessions/.../plan.md + user: "write a plan to make a set F and G (clones of B and D but without the contrast stretch, just the raw images)"). VERY FIRST: re-read current-state.md (hygiene), approved plan (full), AGENT files + standard block if present; narrow forensic read_file + grep on flows list+comment (~245-249), procB full (~889-996), procD full (~1192-1427), procE end + flowProcessors (~1719-1725), "automaticContrastStretch" sites, "val proc[A-E]"; confirm porcelain clean; update TODO first (this entry) + baseline git add + ./build_app; THEN follow phased: 2. update flows list+comment to 7 items (F/G raw clones), 3. insert procF (exact B body -stretch line, flowName=Set F) + procG (exact D body -stretch, flowName=Set G) after procE + extend list to 7 in anchored replace, 4-5. after forensics each, 6. git+builds, 7. END. Scope: ONLY additions of F/G as described (no stretch in their ws path; reuse all B/D logic/dupe pattern/ helpers/ocr filter/custom blue exactly); ZERO changes to A/C/E, stretch fn, valley, reporting builders, masterBuffer copy, helpers, other logic/files. Pre-build forensics + post-edit read/grep + git add kt+TODO before builds; use get-builds-tag.sh for any reset; 1-2 facts in current-state per step; end exact **END OF EXECUTION TURN. Awaiting new directive...** + "results ready to test (tag: <tag>)" + plan ref. (Follows approved plan + prior turn mandates for micro + forensic.)
- [ ] Execution of pump-experiment-de-blue-margins-10pct-top-bottom-20260616-plan.md (VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs) + all prior failure logs read + record lessons on identical first-action skips; compliance re-read of STANDARD BLOCK first 10 lines + last 5 current-state + git --porcelain no .kt + grep TODO pre (no match); narrow forensic read_file (offset/limit ~1315 limit 50 for D custom block including comment 1327 + nt/nb 1333-1334; ~1610 limit 50 for E including 1621 + 1627-1628) + targeted grep for "0\.2 \* hgt|0\.3 \* hgt|20% up + 30% down|red extended 20%|red extended .*30% down" (confirm exactly two hits total for the blue margin verticals, only in D and E custom blocks); re-read first 10 lines of STANDARD BLOCK + last 5 lines of current-state.md; confirm no prior .kt edits in this sub turn; ONLY after 100% gates: update TODO first (this) + verifs + baseline git add .kt + TODO + ./build_app; then Phases 1-3 ultra-micro per plan (D site only, E site only, final forensic); all gates/forensic/grep before/after every edit, git add .kt + TODO.md before each build; scope strictly DE blue margins 10% top+bottom (D ~1327/1333 and E ~1621/1627 sites only) — ZERO edits to ExperimentPumpScreen.kt outside the two described custom margin+comment sites, ZERO to orange/horiz/OCR/prune/B/C/anything else, ZERO to other files; 3-3-3 + anti-doom + literal preflight only via get-builds-tag.sh; end with exact END marker + results ready (tag) + this plan path)
- [ ] Execution of pump-experiment-fix-remaining-font-ml-name-conditionals-and-review-prior-plans-20260616-plan.md (VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs) + all prior failure logs read + record lessons on identical first-action skips; compliance re-read of STANDARD BLOCK first 10 lines + last 5 current-state + git --porcelain no .kt + grep TODO pre (no match); Phase 0: narrow forensic read_file on pBuildHtmlHeader (~2000 limit 15), pBuildHtmlRowDynamic (~2011 limit 100 focusing on subBranches loop ~2033, ML if ~2034, first-column append ~2031 and timing/span/deskew td style), procA (~720 limit 20 for the if), and summary construction (~1570 limit 20 for the if) + targeted grep for the ML th unconditional, ML td containsKey/blank, font-size:6px/span, if \(flowName == flows.first(), if \(f == "Set B", and any other name ifs in builder/procs... ; update TODO first (this) + verifs + baseline git add + ./build_app; then Phases 1-6 ultra-micro per plan (enhance font for full timing shrink, make ML th data-driven in header, make ML slot td match header, remove if(flowName == flows.first()) in procA, make summary if(f == "Set B") data-driven, final forensic); all gates/forensic/grep before/after every edit, git add .kt + current-state.md + TODO.md before each build; scope strictly the listed fixes: effective 1/4 font shrink on full timing block in first column (incl deskewHtml without 14px override), ML columns fully data-driven (no th/td/headers for B-E via hasML precompute from subBranches data presence, no name ifs), clean leftover name conditionals (remove if(flowName == flows.first()) in procA, make summary if(f == "Set B") data-driven on branch data), and review/confirm prior plans' core items are complete (no other changes) — ZERO edits to ExperimentPumpScreen.kt outside the described sites in the Phased, ZERO to procs/helpers/custom/prune/valley/OCR logic, ZERO to other files or logic; 3 [... truncated (2108 chars total)]
  - [x] Phase 0-5 +6 final forensic/greps/builds (6+ total) complete per plan; font 6px full incl deskew no override, ML columns data-driven no th/td/headers B-E, name ifs cleaned, prior plans reviewed complete; state/TODO 1-2 facts each step; all gates; final build success. Results ready.
- [ ] Execution of pump-experiment-b-c-to-6-reds-valley-c-ocr-c-e-new-blue-orange-d-e-remove-valley-d-font-timing-20260616-plan.md (VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs) + all prior failure logs read + record lessons on identical first-action skips; compliance re-read of STANDARD BLOCK first 10 lines + last 5 current-state + git --porcelain no .kt + grep TODO pre (no match); Phase 0: narrow forensic read_file on procB prune/rebuild (~960 limit 30), procC equivalent (~1240 limit 30), doBOrDRetractedBlueAndPD (~598 limit 130), pBuildHtmlRowDynamic first-column (~1805 limit 20), and D/E red handling sites + targeted grep for prune if>6, valley/expandByUniformity, ocrLinesB filter, Deskew/Tilt font, pd_ocr_html... ; update TODO first (this) + verifs + baseline git add + ./build_app; then Phases 1-10 ultra-micro per plan (add prune to B, add prune to C, add valley to C, add OCR to C, add OCR to E, remove valley from D, new blue/orange custom rect for D, new blue/orange for E, font 1/4 on timing, final forensic); all gates/forensic/grep before/after every edit, git add .kt + current-state.md + TODO.md before each build; scope strictly the listed items for B/C/D/E (top-6 prune for B/C, valley expansion for C, OCR for C/E, new custom blue/orange extension for D/E, remove valley expansion from D, font 1/4 size on timing in first column) — ZERO edits to ExperimentPumpScreen.kt outside the described sites in the Phased, ZERO to Set A or report builder beyond font, ZERO to other logic; 3-3-3 + anti-doom + literal preflight only; end with exact END marker + results ready (tag) + this plan path)
  - [x] Phase 0: re-read current (prune/roll), plan, TODO, mandates, standard, 6 logs + AGENT_CONTEXT/GROK; narrow forensic B~950/ C~1070+1240/ retracted~598/ pBuild~1805/ D/E~1985 + targeted greps (if>6 only D/E, valley/expand/ocrLinesB/pdoc r/Deskew/Tilt); gates 100% (porcelain no kt, TODO pre no match); update TODO first; baseline git add + build success.
  - [x] Phase 1: top-6 prune to procB only (narrow/grep; if now B+D/E). git add + build.
  - [x] Phase 2: top-6 prune to procC only (narrow/grep; if now B+C+D/E). git add + build.
  - [x] Phase 3: valley call (retracted) added to procC (narrow/grep on C/retracted; C gets valley blue/orange). git add + build.
  - [x] Phase 4: OCR on C verified via call (narrow/grep ocrLinesB~707 + C calls; pd_ocr set for C; digits filter). git add + build.
  - [x] Phase 5: retracted call added to E post-prune for OCR (narrow/grep; E gets OCR). git add + build.
  - [x] Phase 6: valley retracted call removed from D (narrow/grep on D path; only redOnly; E still has). git add + build.
  - [x] Phase 7: custom rect math (0.2/0.3/0.75) + PD/OCR inline for D (narrow/grep on D site; no valley for D; custom only D). git add + build.
  - [x] Phase 8: identical custom + remove retracted for E (narrow/grep; custom only D/E; retracted calls only B/C). git add + build.
  - [x] Phase 9: font-size:6px span on first-col timing block only (narrow/grep ~2031; 1/4 size; no other font). git add + build.
  - [x] Phase 10: final narrow re-reads/greps (prune if>6 in B/C, valley/OCR in C, OCR in E, valley removed D, custom 20/30/75 only D/E, font:6px only timing, ocrLinesB digits filter, no name ifs); state+TODO; git add + final build + END. All 10+ builds success, scope exact.
- [ ] Execution of pump-experiment-make-ml-column-data-driven-20260616-plan.md (VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs) + all prior failure logs read + record lessons on identical first-action skips; compliance re-read of STANDARD BLOCK first 10 lines + last 5 current-state + git --porcelain no .kt + grep TODO pre (no match); Phase 0: narrow forensic read_file on pBuildHtmlHeader (1804/15) + pBuildHtmlRowDynamic (1815/80 focus ML td ~1838) + targeted grep for ML th/td/?: ''/unconditional... ; update TODO first (this) + verifs + baseline git add + ./build_app; then Phases 1-3 ultra-micro per plan (row ML td to data-driven if/else blank, optional header, final forensic); all gates/forensic/grep before/after every edit, git add .kt + current-state.md + TODO.md before each build; scope strictly the ML column emission in pBuildHtmlHeader and pBuildHtmlRowDynamic — ZERO edits to procs or other files or logic; 3-3-3 + anti-doom + literal preflight only; end with exact END marker + results ready (tag) + this plan path)
  - [x] Phase 0: re-read current (prune/roll), plan, TODO, mandates, standard, 6 logs; narrow forensic header 1804/15 + row 1815/80 +1837/5 + procs read-only + targeted grep ML th/td/?: ""/no name ifs; gates 100% (porcelain no kt, TODO pre no match); update TODO first; baseline git add + build success.
  - [x] Phase 1: row ML td to data-driven if containsKey+nonempty (narrow 1837/5 + grep before/after; old ?: gone); no other changes. git add + build.
  - [x] Phase 2: header th read/grep (1808/5); no edit (row fix primary; ths stay for alignment/structure consistency per plan minimal; no column omission). git add + build.
  - [x] Phase 3: final narrow re-reads header 1804/15 row 1815/80+1837 (ML td now if containsKey+nonempty else blank; no ?: for ML); targeted grep (conditional confirmed, no new name ifs, extraOcr/rawC/PD patterns untouched, procs B-E empty ML via read-only); state+TODO 1-2 facts; git add + final build success + END marker.
- [ ] Execution of pump-experiment-eliminate-set-name-conditionals-20260616-plan.md (VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs) + all prior failure logs read + record lessons on identical first-action skips; compliance re-read of STANDARD first 10 lines + last 5 current-state + git --porcelain no .kt + grep TODO pre (no match); Phase 0: narrow forensic read_file on report builder (~1814-1891) + the five proc* (~897/1187/1001/1332/723) + hoisted (~591-718) + targeted grep for name==/flowName==/dupe comments... ; update TODO first (this) + verifs + baseline git add + ./build_app; then Phases 1-6 ultra-micro per plan (procB dupe cleanup, procD dupe cleanup, procC/procE dupe cleanups, procA if(flowName) removal, report builder name-if to key-presence, final forensic); all gates/forensic/grep before/after every edit, git add .kt + current-state.md + TODO.md before each build; scope strictly the elimination of set-name conditionals and dupe wrappers in the five procs + report builder — ZERO edits to ExperimentPumpScreen.kt outside the described name-conditional/dupe sites, ZERO to shared helpers or other logic; 3-3-3 + anti-doom + literal preflight only; end with exact END marker + results ready (tag) + this plan path)
  - [x] Phase 0: re-read current-state (hygiene prune), plan, TODO, mandates, standard, 6 logs; narrow forensic baseline reads (report ~1814, procs ~897/1001/1187/1332/723, helpers ~591) + targeted grep name/flow/dupe; gates pass (porcelain clean no .kt, TODO grep no match, compliance re-reads); update TODO first (this); verifs; git add .kt + state + TODO; ./build_app (baseline).
  - [x] Phase 1: procB dupe wrapper removal (narrow 995/10 before/after + grep)
  - [x] Phase 2: procD dupe wrapper removal (narrow 1326/10 before/after + grep)
  - [x] Phase 3: procC/procE dupe cleanups
  - [x] Phase 4: procA if(flowName) removal (narrow 731/5 before/after + grep)
  - [x] Phase 5: report builder name-if -> key-presence (multiple narrow reads + grep)
  - [x] Phase 6: final narrow forensic re-reads + grep zero remaining name/flow/dupe; state+TODO 1-2 facts; git add + build; END marker
- [x] Execution of pump-experiment-eliminate-rectf-icrs-float-math-set-a-20260616-plan.md (VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs) + all prior failure logs read + record lessons on identical first-action skips; compliance re-read of STANDARD first 10 lines + last 5 current-state + git --porcelain no .kt + grep TODO pre (no match); Phase 0: narrow forensic reads of procA (720/180) + ML ICRS site (774) + red rebuilds (839) + getFinal call from A (883) + getFinal/expand/takeCrop sites + targeted grep for IcrsMath/RectF/float... ; update TODO first (this) + verifs + baseline git add + ./build_app; then Phases 1-5 ultra-micro per plan (ML ICRS in procA only, red collection/rebuild for A only, getFinal+expand/takeCrop exercised by A, viz for A, final verif); all gates/forensic/grep before/after every edit, git add .kt+state+TODO before each build; scope strictly Set A only (procA red+ML paths + exercised getFinal/expand/takeCrop for A data) — ZERO edits to ExperimentPumpScreen.kt outside the described A sites, ZERO edits to PumpHunk/runDiscoveryPaddle/other procs/B-E paths/global filters; 3-3-3 + anti-doom + literal preflight only; end with exact END marker + results ready (tag) + this plan path)
  - [x] Phase 4: viz read/grep + state+TODO update (A final crops integer clean)
  - [x] Phase 5: final forensic re-reads + grep zero bad ICRS/RectF/float in A red+ML+getFinal/expand/takeCrop; state+TODO; final build + END marker (tag) + plan path.

- [ ] Execution of optimize-pump-scales-and-duplicate-detect-plan.md
  - [x] Phase 1: Update TODO.md
  - [x] Phase 2: Modify runDiscoveryPaddle in ExperimentPumpScreen.kt
  - [x] Phase 3: Modify Outer Loops for Sets A, B, C, D, E
  - [x] Phase 4: Modify Bounding Box Scales List
  - [ ] Phase 5: Verification & Build

- [x] Execution of pump-experiment-reorder-valley-push-20260615-plan.md
  - [x] Phase 0: Preflight & Audit
  - [x] Phase 1: Reorder Set C (`procC`)
  - [x] Phase 2: Reorder Set E (`procE`)
  - [x] Phase 3: Final Verification

- [x] Execution of pump-experiment-takesnapshot-buffer-target-size-logging-20260615-plan.md
- [x] Execution of pump-experiment-fix-coordinate-system-error-plan.md

- [x] Phase 0-2 + final forensic/greps/builds (3 builds) complete per pump-experiment-descriptive-column-labels-stretch-blue-method-20260616-plan.md; flows list + preceding comment updated with descriptive suffixes (A exact "Set A"; B-G exact "(clip edges, expanded)" etc); only intended changed (1 added convention sentence + list); broad/narrow forensics + diff + porcelain + builders re-reads + hasML/toSorted confirm A first/ML; all gates; state/TODO final 1-2 facts; final git add + build success; END marker output. Results ready.
