# TODO

## Active Execution: Pre-Rolling Snapshot UI/Serialization Fix
- [ ] Exclude `"best_plain_pre_rolling"` from serialization keys in `ExperimentAlignmentScreen.kt` (lines 875 and 1983).
- [ ] Extract and display `"best_plain_pre_rolling"` in the `Bin` stage report rendering of `ExperimentAlignmentScreen.kt` (lines 1960-1974).
- [ ] Run `./build_app` to compile and create a new git tag.
