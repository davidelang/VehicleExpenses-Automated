# TODO

## Active Execution: Pre-Rolling HTML Display & Vertical Wide Filter Implementation
- [x] Render the pre-rolling image snapshot in the HTML report table columns.
- [x] Implement the vertical wide filter in `NativeImageUtils.cpp` after the horizontal wide filter but before the CC refresh/small items check.
- [x] Verify compilation success via `./build_app`.
