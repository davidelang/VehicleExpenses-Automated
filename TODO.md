- [ ] VERY FIRST SOURCE EDIT this TODO after re-read current-state.md (hygiene prune first per rules) + FULL plan read (abs /home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/plans/pump-experiment-top-4-redbox-filter-and-redbox-hists-for-all-sets-20260616-plan.md) + AGENT_MANDATES.md + standard-plan-compliance-block.md + the 6+ prior 2026-06-15 failure logs in /home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/implementation-failure-logs/ (lessons: edits/search_replace often on current-state for hygiene or run_terminal before TODO update + gates; TODO grep pre no match; no fresh ritual entry; led to kill + literal preflight TAG=$(./get-builds-tag.sh); git rev-parse "$TAG"; git reset --hard "$TAG" in every case; also duplication-reexec had doom loop on repeat errors without applying different minimal repairs) + AGENT_CONTEXT.md + GROK.md; self-verification gates 100% pass before *any* source edit (incl this TODO): git status --porcelain (M current-state.md only; zero M on .kt) PASS; grep -n for exact plan name in TODO.md (pre zero matches) PASS; narrow forensic read_file (offset/limit on Phase 0 sites: A ~986 limit 40, C ~1123 limit 50, D ~1305 limit 50, E ~1537 limit 50, F ~1783 limit 40, G ~1906 limit 50 + hist C ~1169 limit 60, E ~1583 limit 60 + pBuild ~2349 limit 10) + targeted grep for all " > 6", "subList(6", "prune6", "redboxDataC", "C/E", "6 largest" (prune/hist sites baseline confirmed using 6/C/E/redboxDataC) PASS; re-read first 10 lines of STANDARD BLOCK + last lines of current-state.md PASS; confirm no prior .kt edits this sub turn (status/greps show 6s still present) PASS. Sub-agent ID: 019ed000-execution-subagent-top4-prune-hists-20260616. Update TODO first (this) + verifs + baseline git add kt + TODO + ./build_app (passing files); then Phases 1-16 ultra-micro per plan (prune4 per proc + helper + calls for all sets + filter); all gates/forensic/grep before/after every edit, git add .kt + TODO.md before each build (NEVER git add current-state); scope strictly top-4 prune everywhere + generalized redboxData hists into JSON/metadata for all 7 sets — ZERO to longLived visuals/HTML display for non-C, ZERO to other files/logic; 3-3-3 + anti-doom + literal preflight only via get-builds-tag.sh; end with exact END marker + results ready (tag) + this plan path. Branch: fix-pump-experiment. Plan: /home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/plans/pump-experiment-top-4-redbox-filter-and-redbox-hists-for-all-sets-20260616-plan.md
- [x] Phases 0-5 + final forensic/greps/builds (6+ total) complete per pump-experiment-get-extra-blue-boxes-function-horiz-50-vertical-sweep-20260616-plan.md; helper createBlueAndOrangeHunksFromReds (exact) hoisted; D/E/G replaced with calls using (10..40 step 5).map{it/100f} + 0.5f + updated comments; all 0.75/0.1 single logic gone from the three sites only; downstream aPd/snapshot/OCR untouched; B/F/A/C/reporting zero change; all gates/forensics before/after, git add kt+TODO before builds, builds success per phase + final tag; state/TODO 1-2 facts; END marker output. Results ready.
- [ ] Execution of pump-experiment-get-extra-blue-boxes-function-horiz-50-vertical-sweep-20260616-pl