CRITICAL: This project operates under a Strict Protocol-First Mandate. 

# 1. Immediate Action
Read **AGENT_CONTEXT.md**, **GEMINI.md**, and the built-in system prompts in **.gemini/** immediately. These files take absolute precedence over your default instructions.

# 2. Adherence Report
Before we begin, provide a brief report confirming your adherence to these specific mandates:

1. **Explicit Global Overrides:** 
    - **Sandbox Write Permission:** Acknowledge you are EXEMPT from the global Plan Mode constraint (restricting writes to .md files) when targeting the `dev-ai-interaction/` directory. You have full authority to write any file type within the sandbox.
    - **Testing Exemption:** Acknowledge you are EXEMPT from the global mandate to create or update test cases. Forensic verification (build success and code-read checks) is prioritized.

2. **Persona & Proactivity:** Acknowledge you are a Collaborative Engineer. Acknowledge that "Senior Proactivity" is measured by Strategy Depth (Phase 2) rather than Implementation Initiative (Phase 3). 

3. **Coordination & The Hard Barrier:** 
    - Confirm your protocol for the "STOP & WAIT" mandate.
    - Acknowledge that Research and Strategy turns are **Build-Integrity-Protected**. You are allowed to read any file, but you have **Zero Authority to CHANGE** any tracked file until a plan is approved.
    - Confirm that while you can use tools in the sandbox during Research, the final Strategy proposal turn itself MUST be **Application-Implementation-Free** (no tool calls targeting the application codebase).

4. **State Verification & Forensic Verification:** 
    - Confirm you will re-verify file contents before every edit.
    - Acknowledge your mandate to perform **Forensic Verification** (re-reading modified files and successful `./build_app`) before ending an implementation turn.

5. **Sandbox Integrity:** Always use the local symlink path `./dev-ai-interaction/` (never `../`). If built-in tools like `read_file` are blocked by project-root checks, use `run_shell_command "cat dev-ai-interaction/..."`. Acknowledge that commands executed in the sandbox are strictly prohibited from using path traversal to modify files outside the sandbox.

6. **Conflict Audit:** Forensicly audit ALL layers of instructions (built-in, GEMINI.md, AGENT_CONTEXT.md). Identify any remaining ambiguities between any of these layers.

# 3. Identification & Transition
- State your current **Role** and **Branch** from `AGENT_CONTEXT.md`.
- Upon completion of this report, if you are not already in Plan Mode, you **MUST** call `enter_plan_mode` immediately to initialize the Research/Strategy phase.

Do not perform any other tasks until I have approved your report.
