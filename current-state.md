- Branch: fix-pump-experiment
- Current builds tag: fix-pump-experiment/builds @ 38d2f177
- Last completed: pump-experiment-eliminate-set-name-conditionals-20260616-plan.md (full ritual, 6 builds, name conditionals/dupe wrappers eliminated; ML column still unconditional in builder; results ready)
- Execution of pump-experiment-make-ml-column-data-driven-20260616-plan.md started (user execute magic). Master baseline: .kt clean, tag 38d2f177, state pruned. Spawning narrow bg Execution Sub-agent (strict ritual: TODO as VERY FIRST + gates + all 6 06-15 failure logs + plan Phased + scope ML column data-driven only in builder). Sub does Phase 0 forensic (header/row) + baseline build first. 1-2 facts only. Rolled: prior eliminate-conditionals phases + rectf (see last plans + git since tag).
- Phase 0 gates + re-reads (current pruned/rolled, plan abs, TODO first-edit, mandates, standard, 6 logs) + narrow forensic (header@1804/15, row@1815/80+1837/5, procs@875A/988B/1172C/1314D/1513E read-only) + targeted grep (ML th@1809 td@1838 ?: "" ; no builder name-ifs for ML; data-driven containsKey patterns present for extraOcr/rawC) pass 100% (porcelain: M state only no .kt; TODO grep pre no match). Baseline build SUCCESS (new tag). 1-2 facts only.
- Phase 1: ML td in row now strictly data-driven (if containsKey("ML") && non-empty; else blank td); post narrow read 1837/5 + grep confirm (no ?: "" for ML, other ?: /containsKey untouched, header ths unchanged, no name ifs added); git add kt+state+TODO + build next. 1-2 facts only.
- Phase 2: header narrow read 1808/5 + grep (ths unconditional at 1809); no edit (per plan: row primary suffices, keep ths for table alignment; B-E will have blank ML tds); build next. 1-2 facts only.
- Phase 3 final: narrow re-reads + grep confirm ML td strictly data-driven via containsKey+non-empty (blank else; no broken ?: "" for B-E); header consistent; no name ifs; other patterns/procs untouched; final git add + build + END. New tag. 1-2 facts only.