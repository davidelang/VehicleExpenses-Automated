- Branch: fix-pump-experiment
- Current builds tag: fix-pump-experiment/builds @ 425d5a0e
- Execution of pump-experiment-heatmap-hist-in-redbox-fn-plus-perchar-recognition-probs-in-json-20260616-plan.md started (user execute magic, abs sandbox path). Sub 019ed1b4 (set 1) cancelled harness (stagnation 42 calls, no TODO). Repair sub 019ed1b7 did TODO + Phase 1 (hist IntArray(100) + bin loop in legacy per forensic at 1122) + claimed Phase 2 in state; killed + literal preflight (landed 425d5a0e). State pruned/rolled (old blue + sub phase details removed for hygiene). Master now completing remaining phases 2-8 (wire hist in wrapper + runDiscoveryPaddle metadata, per-char in Native/RecStage + ocr_lines + lanes, JSON, final build) with full forensic gates, git add .kt+TODO (pass files incl current-state to build_app), 1-2 facts/step. 1-2 facts only. Plan: /home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/plans/pump-experiment-heatmap-hist-in-redbox-fn-plus-perchar-recognition-probs-in-json-20260616-plan.md
- Phase 2: hist return wired (public processPaddleHeatmap now : Pair<List<TextBlock>, IntArray?>, nativeBoxes/ null paths return Pair(..., null), legacy : Pair + final "return Pair(results, hist)", call site in runDiscoveryPaddle destructures "val (rawBlocks, hist) = ..."); narrow forensic after (reads + grep) PASS (Pair sig/returns/hist return/destructure present; original box logic + hist loop from Phase 1 intact); ready Phase 3 (metadata record + build). 1-2 facts only.
- Phase 3: metadata["heatmap_hist_${scale}"] = JSONArray(hist.toList()).toString() added after destructure in runDiscoveryPaddle (will flow to JSON via branch metadata); narrow forensic after + grep PASS (put line present, import already); callers .first fixes (Phase 4) in prior repair build; build for 2+3+4 next. 1-2 facts only.
- Phase 3 build SUCCESS + tag d7fcf099 (from 133fce63); hist now recorded per-scale in metadata/JSON for all redbox fn paths. Hist part of plan complete. Starting per-char probs (Phase 5 Native extend/capture + 6-8 wiring to ocr_lines + lanes + JSON). 1-2 facts only.
- Phase 5: RecStageResult + OcrResult extended with perCharProbs: String = ""; capture in both processOcr (general CTC) and processOcrNumeric (constrained, only passed/emitted) using maxVal at emit time, collected as "ch:0.98,ch:0.87..." aligned to final text; passed through recognize OcrResult constructions; narrow forensic + grep PASS; ready Phase 6 (blue/orange wiring in pump). 1-2 facts only.
