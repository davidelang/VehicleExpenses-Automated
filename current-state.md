- Branch: fix-pump-experiment
- Current builds tag: fix-pump-experiment/builds @ 08055551 (from ./get-builds-tag.sh + git rev-parse)
- Current approved sandbox plan: /home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/plans/pump-experiment-heatmap-hist-in-redbox-fn-plus-perchar-recognition-probs-in-json-20260616-plan.md
- Progress (one-line): In progress on heatmap_hist + per-char recognition probs integration into redbox / ocr / JSON paths (per approved plan). Hist recording in metadata/JSON complete for redbox fn paths; per-char probs extension started.
- Key code state facts:
  - heatmap_hist_${scale} now recorded in branch.metadata (as JSONArray string) and flows to reports/JSON for redbox fn paths.
  - RecStageResult + OcrResult extended with perCharProbs: String (captured in processOcr / processOcrNumeric paths using maxVal; aligned "ch:conf" format).
- Device / reports / logs pointers (critical for new agents):
  - Latest pump experiment reports, JSON outputs, and analysis files: fetched to dev-ai-interaction/ (use analyze_new_report.py and related scripts there for inspection and processing).
  - Runtime, crash, and current device logs: live on the physical device (not reliable in local filesystem *log or *txt files). Fetch via adb (adb logcat for live; the fetch scripts in dev-ai-interaction/ produce adb_crash_log.txt / adb_logs.txt). adb logcat is explicitly allowed for read operations.
- Older execution / sub-agent / phase narrative: Rolled to ENGINEERING_LOG.md (see the [2026-06-16] dated section for the recent pump phases activity, sub-agent IDs, and repair details). Full history also in the approved plan file + git since the baseline builds tag. Do not re-ingest old narrative here.
- Last steps (long mechanical execution; pruned to most recent):
  - Step (recent): Hist return and metadata wiring complete + successful build (tag d7fcf099 at time of step).
  - Step (recent): perCharProbs capture extended in OCR paths; forensic verification passed; ready for subsequent wiring phases.
- Open questions / remaining: Per-char probs wiring to ocr_lines / lanes / JSON (later phases of current plan); blue/orange special paths; final verification build and handoff. See the designated plan for exact remaining steps.
- Phase 5 build SUCCESS + tag 08055551 (from d7fcf099); per char now captured in recognition (Native) and available on OcrResult. Starting Phase 6 wiring to ocr_lines/pd_ocr_html (blue/orange) + Phase 7 lanes. 1-2 facts only.
- Phases 6/7: per char wired to blue/orange ocr maps (embedded "[probs:9:0.98,...]" in asis/d texts for all variants/retracted/D/E/G) so ocrLines + pd_ocr_html have it (in metadata/JSON); lane in performHunk (Paddle path) embeds in PumpHunk text so PathResult/lane results have it in JSON pathResults; forensic/grep PASS (17 sites updated, no dup); build next. 1-2 facts only.
