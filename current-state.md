- Branch: fix-pump-experiment
- Current builds tag: fix-pump-experiment/builds @ cd1dce16 (Phase 1)
- Phase 0: gates 100%, TODO first, baseline build success @9741e56f. Phase 1: pre/post narrow/grep+diff (list+1 sentence only; A exact; suffixes once in list/comment; procs short ok); state update; git add kt+TODO; build success. Phase 2: broad grep (descriptives only comment+list; short Set X only in proc flowName= vestigial; builders use $flow/$name from list, hasML=first A, toSorted* order A first); re-reads builders+region (toSortedSet/first for hasML, toSortedMap for perSetTilts/$name); porcelain clean tracked (kt+TODO committed by build); final state+TODO 1-2 facts/completion; git add kt+TODO; final build. All 3 builds success, scope exact, verifs pass. Results ready. 1-2 facts only.