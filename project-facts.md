# project-facts.md — Stable "where things live" facts (app worktree)

Contains only verifiable locations and structure facts that remain true across work on this tree (after merge + new worktree for different effort). No branch names, tags, commit hashes, or effort narrative.

Read in full early on startup/new cycle to avoid find/discovery commands.

## Sandbox (shared via symlink)
- `dev-ai-interaction -> ../dev-ai-interaction` (separate sandbox git for analysis/plans/paddle-fork tooling)
- Absolute path for writes: `/home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/`
- `dev-ai-interaction/plans/` — designated active plan (user names exact file)
- `dev-ai-interaction/historical-plans/` — archived plans
- `dev-ai-interaction/implementation-failure-logs/` — scan on startup / "new planning cycle"
- `dev-ai-interaction/.planning-agent-prompt.txt` — planner restart prompt from master
- `dev-ai-interaction/paddle-build/patches-int8/` — Paddle-Lite INT8/u8 fork patches (`apply_int8_patches.sh`, `Dockerfile.int8`)
- `dev-ai-interaction/paddle-build/Dockerfile.int8` — post-build `patchelf --set-soname libpaddle_lite_jni.so` via `/workspace/set_jni_soname.sh` for arm jni outputs
- `dev-ai-interaction/scripts/deploy-golden-pump-photos.sh` — push GOLDEN_SUBSET and/or flat pump zips to device
- `dev-ai-interaction/research/photos/pump/` — pump experiment source photos; `pump-zips/` holds generated flat zips
- `dev-ai-interaction/research/imagine-icon-candidate/` — launcher icon Imagine source: README with Grok Imagine post URL; masters `app-icon-master-1024.png` / `app-icon-master-512.png`; density export under `android-export/`
- `dev-ai-interaction/rclone-build/` — sandbox Go+NDK+gomobile image (`Dockerfile`), `build_lite.sh` (drive+local only), `build_full.sh` (all rclone backends), `build_photo.sh` (curated photo backends — compile-out local/memory/http and niche types per `RcloneProviderCatalog.COMPILED_OUT_TYPES`); outputs `librclone_lite.aar` (~32MB), `librclone_full.aar` (~87MB), `librclone_photo.aar` (~76MB; arm64 `libgojni.so` ~77MB)
- App-relevant durable docs/scripts belong in tracked `docs/` or `app/` — not sandbox-only

## Tracked documentation (`docs/` in app worktrees)
- `docs/specs/` — hard requirements; if docs and code disagree, **docs are authoritative**
- `docs/reference/` — documents current code/artifacts; if docs and code disagree, **code is authoritative**
- `docs/obsolete/` — retired approaches kept for possible future reuse (not current contract)
- Paddle host tooling guide: `docs/specs/HOST_PADDLE_USE.md` (Python env, `opt` tool, dynamic shapes)
- Paddle fork rebuild: `docs/specs/PADDLE_BUILD.md`, `docs/specs/BUILD_ENVIRONMENT.md`
- Coordinates: `docs/specs/ISOTROPIC_COORDINATE_SPEC.md`
- Launcher icon: `docs/reference/APP_LAUNCHER_ICON.md`
- Reports metrics ($/mi, volume display): `docs/reference/REPORTS_METRICS.md`
- Self-hosted sync setup cheatsheets: `docs/reference/self-host/` (INDEX, `photos/*`, `tabular/*`, `vendor-links/VENDOR_LINKS.md`); in-app links via `ui/util/SyncSetupDocs.kt`
- Sync behavior reference (recovery, duplicates, LWW): `docs/reference/SYNC_BEHAVIOR.md`; user summary in `docs/reference/USER_GUIDE.md` sync section
- End-user manual: **edit** `docs/user-manual.md`; **browsers/app open HTML** (`docs/user-manual.html`, assets `app/src/main/assets/user-manual/`). Pipeline doc: `docs/reference/USER_MANUAL_BUILD.md`. Regenerate: `./scripts/render-user-manual.sh`. Never send end users to raw `.md` URLs. Help/About → `UserManualDocs.openFullManual` → `UserManualActivity` WebView. Online: jsDelivr `docs/user-manual.html` on master (`UserManualDocs.ONLINE_HTML_URL`).

## At worktree root
- `MASTER_AGENT_MANDATE.md` — master worktree only: PR review + merge integration protocol (special handling for ENGINEERING_LOG, TODO, project-facts)
- `ENGINEERING_LOG.md` (append-only; use ONLY `./append-to-engineering-log` or `@file` wrapper)
- `project-facts.md` (this file)
- `TODO.md` (backlog)
- Launchers: `run-grok*`, `build_app`, `get-builds-tag.sh`, `append-to-engineering-log`
- `update-rules.sh` (from orchestration root), `set-worktree-perms`, `set-sandbox-perms`, `setup-project`
- `.grok/config.toml` + `.grok/hooks/`
- `project.config.example`

## Application source (stable layout)
- `app/` (Android root)
- `app/src/main/AndroidManifest.xml` — `<application android:largeHeap="true">`
- Package: `app/src/main/java/com/davidlang/vehicleexpensesautomated/`
- Core OCR harness: `ui/util/OcrEngine.kt`, `OcrHarness.kt`, `NativePaddleEngine.kt`, `IcrsMath.kt`
- Production Paddle path: `NativePaddleEngine.PROD_PATH_ID` = `uint8_fp16_u8` (raw uint8 feed → fp16 compute; det uint8 heatmap thresh 0)
- Production models: `app/src/main/assets/paddle/prod_u8fp16/` (`det_*`, `rec_v3_*`, `rec_numeric_*` for armv8 + x86_64 naming)
- Host model scripts (checked in): `app/src/main/assets/paddle/scripts/` (`convert_mono.py`, `optimize_models.sh`, `optimize_mono_int8_models.sh`)
- Paddle runtime binaries: `app/src/main/jniLibs/{arm64-v8a,armeabi-v7a,x86_64}/`, `app/libs/PaddlePredictor.jar`
- JNI sizing (prod path): arm64-v8a tailored ~1.6MB; x86_64 slim jni + `libpaddle_light_api_shared.so`; armeabi-v7a fat multi-path lib (interim)
- Volume units: `ui/util/VolumeUnits.kt` — preferred G/L in prefs; `FuelEntry.gallons` stores volume in preferred unit (field name legacy)
- Currency: `ui/util/CurrencyCodes.kt` — symbol/code → ISO 4217 stored form; row `currency` on `FuelEntry` + `ExpenseEntry` (blank = legacy/settings default at display); no offline FX
- Quick fill UI: `ui/fuel/QuickFillupScreen.kt` — 3-panel A/B/C; multi-photo JSON in `photoUrl` (dash/pump); persists selected fill currency at save; `ui/components/CameraPreview.kt` zoom callback
- Pump cost/vol OCR: `ui/util/PumpRoleBandClassifier.kt` (Y-band role pools + band-gated pair; global-pair fallback); `PumpCostVolUtils.classifyCostVolFromBoxOcr` delegates; `PumpOcrSettings.kt` prefs in `vehicle_settings` (`pump_max_red_boxes` default 8, `pump_label_y_band_extra_fraction`, ratio band lo/hi default 2/30); Quick Fill path via `OcrHarness` uses Context-aware settings; advanced fields in `SettingsScreen.kt` (ratio under experiment toggle)
- Quick Fill debug store: `ui/util/QuickFillDebugStore.kt` — app-private `filesDir/quick_fill_debug/` (session packages) and `filesDir/crash_reports/` (uncaught-exception tombstones); prefs `debug_quick_fill`, `debug_quick_fill_max_sessions` in `vehicle_settings`
- FileProvider for attachments/CSV export: `androidx.core.content.FileProvider` authority `${applicationId}.fileprovider`; path roots in `app/src/main/res/xml/file_paths.xml` (internal `files/` + external `Downloads/`)
- Expense local photo pref: `save_expense_photos` in `vehicle_settings` (default true); honored in `ui/expenses/ExpenseEntryScreen.kt` capture/save path
- Reports UI: `ui/reports/ReportsScreen.kt` — per-vehicle summary, last-5 full-fill legs, expense categories; money uses row currency when set; mixed-currency aggregates show per-currency subtotals (no silent cross-currency sum); $/mi n/a when mixed
- Expenses UI: `ui/expenses/ExpenseEntryScreen.kt`, `ExpenseListScreen.kt`, `ExpenseEntryMode.kt` — vehicle dropdown, camera/zoom, currency on amount; create vs edit via typed `ExpenseEntryMode`; DB v8 adds `vendor` + `odometer`
- Tabular sync library: `data/sync/tabular/` — public `TabularShareApi` + `TabularShareBackend` + `TabularSchema` (canonical headers/tab names/row DTOs); `TabularBackendRegistry` dispatches by `SpreadsheetProvider`; backends in `tabular/internal/`: `GoogleSheetsTabularBackend`, `ExcelGraphTabularBackend` (`GraphExcelClient` + MSAL via `MicrosoftOneDriveAuth`), `EtherCalcTabularBackend` (`EtherCalcClient`), row-DB backends (`BaserowTabularBackend`, `NocoDbTabularBackend`, `PocketBaseTabularBackend`, `SupabaseTabularBackend`, `AirtableTabularBackend`, `FirebaseTabularBackend` via shared `RowDbTabularBackend` + per-vendor clients including `FirebaseTabularClient`), grid backend `ZohoSheetTabularBackend` (`ZohoSheetClient` + `ZohoSheetAuth` OAuth), `CsvZipTabularBackend` (hidden import/export adapter), `OtherTabularBackendStub` (unknown Other subtype fallback), `DeferredTabularBackendStub` (OnlyOffice/Collabora deferred — no headless row API); `TabularOtherProviderCatalog.kt` — Other kind picker groups; `RowDbTabularConfig.kt` / `FirebaseTabularConfig.kt` / `ZohoSheetConfig.kt` — per-provider `configJson`; cheatsheets `docs/reference/self-host/tabular/firebase.md`, `zoho-sheet.md`; Hilt: `di/TabularShareModule.kt`; app coordinator/UI call `TabularShareApi` only — no direct `GoogleSheetsClient` in `SpreadsheetSyncCoordinator`
- Data sync: `data/sync/CsvManager.kt` (thin wrapper → `TabularShareApi.exportCsvZip` / `importCsvZip`), `GoogleSheetsClient.kt` (Google Sheets SDK, used only inside `GoogleSheetsTabularBackend` + browse/create UI), `SpreadsheetSyncCoordinator.kt` — quoted CSV fields; expense/fuel column parity via `TabularSchema`; bidirectional LWW merge by `syncId`; vehicle merge overlays definition fields (ICRS crops, `landmarkTextBlocksJson`, `cloudManifest`, local photo paths) from the non-winning side when the LWW winner is thin; `VehicleRepository.upsertFromSync` / fuel/expense upserts preserve readable local photo paths when sheet rows omit them; sheet write-back sorts oldest-first (Fuel `timestamp`, Expenses `date`, Vehicles `name`; tie-break `syncId`) and uses incremental append/update/skip via backend port (full rewrite + clear trailing on disorder); after vehicles tab merge, best-effort `PhotoBackupCoordinator.downloadMissingVehicleAssets()` pulls missing reference images (errors log only)
- Google auth (legacy Sign-In boundary): `data/sync/GoogleLegacySignIn.kt` — single `@Suppress(DEPRECATION)` adapter for Drive + Sheets; `GoogleDriveAuth.kt`, `GoogleSheetsAuth.kt` delegate here pending Credential Manager migration
- Google Sheets auth: `GoogleSheetsAuth.kt`, `SheetsAuthRecovery.kt` — `Scopes.SPREADSHEETS` only (separate from Drive photo backup); account hint pref `sheets_account_name`; `UserRecoverableAuth*` recovery via Spreadsheet Sync screen
- Rclone native lib: `app/libs/librclone.aar` (photo-curated backends from sandbox `build_photo.sh` / `librclone_photo.aar` ~76MB; reference full baseline `librclone_full.aar` ~87MB); Java API `gomobile.Gomobile.rcloneInitialize()` / `rcloneRPC(method, inputJson)` / `rcloneFinalize()`; load via `data/sync/RcloneLoader.kt` (`libgojni` from AAR or `filesDir/expanded_rclone/libgojni.so`); runtime wrapper `data/sync/RcloneRuntime.kt` (`config/setpath`, `config/listremotes`, `config/create`, `config/update`, `config/delete`, `config/providers`, RPC helper); provider catalog `data/sync/RcloneProviderCatalog.kt` (dynamic + curated fallback); per-dest conf at `filesDir/rclone/<destId>/rclone.conf` (in-app create or SAF import; `RcloneConfStorage.ensureEmptyConf`; never log secrets)
- Google Drive + OneDrive + S3 + Other photo backup: `data/sync/GoogleDriveAuth.kt` (`drive.file` scope), `MicrosoftOneDriveAuth.kt` (MSAL Android; `Files.ReadWrite` + `User.Read`; never log tokens; pref `onedrive_account_name`), `RcloneOneDriveSetup.kt` (managed `onedrive` remote `od_<destId8>`), `RcloneS3Setup.kt` (managed `s3` remote `s3_<destId8>`; `S3ProviderPreset` for AWS/Wasabi/R2/MinIO/Other), `DriveAuthRecovery.kt`, `CloudManifest.kt` (`provider` = `google_drive` or `rclone` wire protocol; rclone `fileId` = object key under prefix; no cross-provider `getFileId` fallback for rclone dests), `PhotoSyncBackend.kt` port with `GoogleDrivePhotoBackend.kt` + `RclonePhotoBackend.kt` + `PhotoBackendRegistry.kt`, `PhotoBackupCoordinator.kt`, `PhotoBackupWorker.kt`, `PhotoBackupManager.kt`; Drive path uses `PhotoStorageManager.kt`; rclone uses gomobile RPC (`operations/copyfile`, `operations/list`, `operations/mkdir`, `operations/deletefile`); **Test connection contract** (all providers): write → list → read probe `.ve_probe_<timestamp>.txt`, delete best-effort; UI `ui/settings/PhotoBackupScreen.kt` — 4-way picker **Google Drive / OneDrive / S3 / Other** (`PhotoProvider` includes `S3`; legacy JSON `rclone` → `OTHER`); OneDrive: MSAL + path prefix; `PhotoBackupCoordinator` calls `RcloneOneDriveSetup.refreshTokenIfPossible` before test/sync/upload/download (background + manual, not UI-only); S3: keys/bucket/region/endpoint form (no `s3` under Other); Other: kind-grouped rclone UI (`RcloneProviderCatalog.kt` kinds + denylist; `RcloneRemoteDialogs.kt` wizard); `RcloneProviderCatalog.UI_HIDDEN_TYPES` hides wrappers + drive/onedrive/s3 from Other create; `COMPILED_OUT_TYPES` denylist for pruned backends; footer “Powered by rclone…” on Other; MSAL config `res/raw/msal_auth_config.json` (placeholder until app registration); manual **Sync now** = `PhotoSyncMode.FULL`; background `PhotoBackupWorker` = `PhotoSyncMode.PENDING_ONLY`; `PhotoStorageManager.existingVehicleRefPath` / `rebindOnDiskVehicleRefs`; enqueue after save; stores **binary images only**; **multi-dest pending:** upload = local readable + `!hasEntryForDest`; download `getFileId` may fall back across Google Drive dests only; `bindLocalDestAfterDownload` adds local dest entry with correct provider; obsolete `vehicle_landmarks` stripped via `stripObsoleteRoles`; vehicle manifest updates use `VehicleRepository.updateVehiclePreservingTimestamp`
- Vehicle crops + `landmarkTextBlocksJson` sync via **Vehicles** sheet row / `Vehicles.csv` (not Drive files); `GoogleSheetsClient.VEHICLE_HEADERS` includes ICRS crop columns + **Landmark Text Blocks JSON** before **Cloud Manifest**
- Sheet tabs: `Vehicles`, `Expenses`, per-vehicle `Fuel - {vehicle name}` (sanitized via `TabularSchema.fuelTabName`); first identity column **Sync ID**; fuel/expense tabs include **Vehicle Sync ID** (+ legacy Vehicle ID fallback); on vehicle rename, `SpreadsheetSyncCoordinator.syncFuelTabs` renames orphan fuel tabs (`GoogleSheetsClient.renameTab`) or LWW-merges both tabs then deletes the old one; offline rename hint pref `fuel_tab_rename_hint_{vehicleSyncId}` in `FuelTabRenameHintStore` (set on `VehicleRepository.updateVehicle`, consumed at sync)
- CSV export/import (`CsvManager.exportToZip` / `importFromZip`): ZIP mirrors sheet tabs — `Vehicles.csv`, `Expenses.csv`, per-vehicle `Fuel - {name}.csv` (`TabularSchema.fuelTabName`); columns via `TabularSchema` headers + row mappers; fuel/expense headers include **Currency** (after Cost/Amount); fuel/expense export fills **Vehicle Sync ID**; rows oldest-first (vehicles by name, fuel by timestamp, expenses by date; tie-break `syncId`); includes deleted rows for backup parity; import accepts legacy `Fuel_entries.csv` / `Expense_entries.csv` and resolves vehicle by Vehicle Sync ID column, fuel tab name, or Vehicle ID
- Sync orchestration: `SpreadsheetSyncCoordinator.syncNow()` (manual Sync now + workers; single-flight `Mutex`); `SyncIdBackfill.runIfNeeded()` gated behind MainActivity upgrade splash until `sync_id_backfill_done`; background `SyncManager.kt` / `SyncWorker.kt` and `PhotoBackupManager.kt` / `PhotoBackupWorker.kt` schedule from `SyncDestinationStore` using `frequencyMinutes` (min **15**, max 24×60; legacy `frequencyHours` migrated on load); settings UI shows **hours** slider 0.25–24 h via `SyncFrequencyUi`; real `PeriodicWorkRequest` in minutes with `ExistingPeriodicWorkPolicy.REPLACE` on settings save and short `initialDelay` (~2 min); cold start schedules periodic only after backfill (no immediate enqueue); settings screens reschedule on save via `SpreadsheetSyncViewModel.rescheduleBackgroundSync()` / `PhotoBackupViewModel.rescheduleBackgroundBackup()`; per-destination last failure in `SyncFailureStore.kt` (red settings summary + app-bar error icon → Settings)
- Hilt WorkManager workers: `@HiltWorker` + `@AssistedInject` on `SyncWorker.kt`, `PhotoBackupWorker.kt`; requires `androidx.hilt:hilt-work` **and** `ksp("androidx.hilt:hilt-compiler")` in `app/build.gradle.kts` (in addition to Dagger `hilt-compiler`); `VehicleExpensesApplication` implements `Configuration.Provider` with injected `HiltWorkerFactory` (sole WM init path — no `WorkManager.initialize` in DI); manifest disables `WorkManagerInitializer` via Startup meta-data remove
- Sync device identity: `data/sync/SyncIdentity.kt` — pref `sync_device_id` (UUID) in `vehicle_settings`; **primary merge key** = `syncId` on each row (deterministic backfill for cloned DBs via `SyncIdGenerator.kt`)
- Room DB: `data/db/AppDatabase.kt` version **13**; migrations `MIGRATION_8_9` … `MIGRATION_12_13` in `di/DatabaseModule.kt` (v13 table-rebuild drops legacy `receiptImagePath` + `isIcrs` columns)
- Sync columns on `Vehicle`, `FuelEntry`, `ExpenseEntry`: `syncId`, `originDeviceId`, `updatedAt` (ms LWW), `deleted`, `deletedAt`; `cloudManifest` on all three entity types; expense local image field is `photoUrl` only; expense multi-vehicle association via `vehicleSyncIdsJson` (JSON array of vehicle `syncId`s, primary first) + primary `vehicleId`; multi-page expense receipts via `photoUrl` JSON (`ExpensePhotoUrls.kt` — `[{"i":0,"uri":"…"},…]`; page 0 CloudManifest role `expense_receipt`, k≥1 `expense_receipt_{k}`); tabular **Vehicle Sync IDs** column on Expenses (`TabularSchema.EXPENSE_HEADERS`); reports v1: full expense amount counts on primary vehicle only for money totals; expense still lists under each associated vehicle
- Sync destination registry: `data/sync/SyncDestinationModels.kt`, `data/sync/SyncDestinationStore.kt` — JSON in `vehicle_settings` pref `sync_destinations_json` (spreadsheet + photo lists); `allSpreadsheet()` / `allPhoto()` / `enabledSpreadsheet()` / `enabledPhoto()` / `upsertSpreadsheet` / `upsertPhoto` / `removeSpreadsheet` / `removePhoto`; soft cap `MAX_DESTINATIONS_PER_TYPE` = 5; primary helpers `spreadsheetDestination()` / `photoDestination()` = first enabled configured else first; legacy migrate flag `sync_destinations_migrated`; pending count pref `sync_pending_count` (photo pending = sum across all configured Drive dests)
- Sync orchestration multi-dest: manual + background spreadsheet sync loops all `enabledSpreadsheet()` sequentially (aggregate message; success only if all succeed); photo backup loops all `enabledPhoto()` sequentially; `SyncManager` / `PhotoBackupManager` schedule one periodic worker with min `frequencyMinutes` + strictest wifi/charging across enabled dests
- Settings UI: `ui/settings/SettingsScreen.kt` — main summary rows show N configured / M on for spreadsheet + photo; sub-routes `settings/spreadsheet_sync` (`SpreadsheetSyncScreen.kt` orchestrator + `SpreadsheetDestEditForm.kt` + `SpreadsheetDestPickers.kt` + `RowDbSpreadsheetForm.kt` + `FirebaseSpreadsheetForm.kt` + `ZohoSheetSpreadsheetForm.kt`; Zoho OAuth redirect `ZohoOAuthRedirectActivity`) and `settings/photo_backup` (`PhotoBackupScreen.kt` orchestrator + `PhotoBackupDestEditForm.kt`); shared composables `SyncDestinationCommonUi.kt`, `SyncDestinationScheduleUi.kt`; spreadsheet: 4-way picker **Google Sheets / Excel / EtherCalc / Other** (`SpreadsheetProvider`); Other → kind groups → per-provider setup; photo: 4-way picker Google Drive / OneDrive / S3 / Other; list + add/edit/remove per dest; wired in `MainActivity.kt`
- Sync frequency migration: `data/sync/SyncFrequencyMigration.kt` — legacy JSON `frequencyHours` → `frequencyMinutes` on load; UI slider via `SyncFrequencyUi`
- Google destination browse UX: `ui/settings/GoogleDriveBrowserDialog.kt` + `data/sync/GoogleDriveBrowserClient.kt` — in-app Drive list/create modal for spreadsheet pick (`SpreadsheetDestEditForm.kt`: Sheet URL + browse) and Drive folder pick (`PhotoBackupDestEditForm.kt`: optional folder URL + browse; `folderId` primary bind)
- Experiment UI: `ui/experiment/ExperimentPumpScreen.kt`, `ExperimentAlignmentScreen.kt`
- Pump experiment regression test set (26 images): `PROBLEM_IMAGES_UINT8_U8` in `ExperimentPumpScreen.kt` + Problem Images button
- Pump experiment binPeak/histogram details: `ExperimentPumpScreen.kt` (`PUMP_*`, `BIN_PEAK_BINARIZE_DELTA`, `findPeaksFromHistBins`, `NativeImageUtils.calculateHistogramWithThresholdH` in `NativeImageUtils.cpp`)
- Hybrid stretch helpers: `ui/util/OdometerOcrUtils.kt` — `getClipStretchLowHigh`, `getValleyPeakGrays`, `applyValleyPushWithGrays`
- Data/UI: `Vehicle.kt`, `VehicleViewModel.kt`, `ManageVehiclesScreen.kt` — form rehydrates from Room when selected vehicle definition fields change (not only id); auto-deserializes `discoveryResults` from `landmarkTextBlocksJson` when reference image size is known
- Quick Fill / OCR: reads `VehicleViewModel.vehicles` Room Flow at capture time — after spreadsheet sync, definitions are in Room without opening Manage Vehicles

Update only with new stable location facts valid for future unrelated work. Current-effort details go in the active plan or ENGINEERING_LOG.md.